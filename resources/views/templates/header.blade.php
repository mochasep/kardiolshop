<div class="jumbotron jumbotron-fluid" style="background-color: #AED581;">
    <div class="container">
        <h1 class="display-1">Kardi Online Shop</h1>
        @if (isset($subtitle))
            <p class="lead">{{ $subtitle }}</p>
        @endif
    </div>
</div>